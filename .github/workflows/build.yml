name: build

on:
  push:
    branches:
      - 'main'
      - 'rr/**' # "rr" stands for "remote run"
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * *' # every day at 00:00

jobs:

  build-debug:
    strategy:
      # fail-fast: false # Disable fail-fast in matrix
      matrix:
        # https://docs.github.com/en/actions/reference/runners/github-hosted-runners#standard-github-hosted-runners-for-public-repositories
        # https://github.com/actions/runner-images/blob/main/images/macos/macos-15-arm64-Readme.md
        # https://github.com/actions/runner-images/blob/main/images/macos/macos-26-arm64-Readme.md
        #
        # Xcode versions:
        # - https://en.wikipedia.org/wiki/Xcode
        # - https://xcodereleases.com/?scope=release
        #
        # macos-14 was dropped: GitHub began deprecating that image on 2026-07-06, fails jobs during
        # brownouts through October, and retires it on 2026-11-02.
        # https://github.com/actions/runner-images/issues/13518
        os: [macos-15, macos-26]
    name: build-debug
    runs-on: ${{ matrix.os }}
    steps:
      - run: env # Debug
      - uses: actions/checkout@v6
      - run: ./.github/gh-actions-runner-xcode-select.sh
      - run: brew install swiftly
      - run: swiftly init --skip-install --assume-yes --verbose && swiftly install
      - run: ./script/build-debug.sh
      - run: ./script/test.sh

  # We build release artifacts only on the latest macOS versions because:
  # 1. It cuts the build time twice on GH Actions
  # 2. The latest Xcode version is not available on old macOS, and old Xcode versions bundle too old Swift version
  build-release:
    strategy:
      # fail-fast: false # Disable fail-fast in matrix
      matrix:
        os: [macos-15, macos-26]
    name: build-release
    runs-on: ${{ matrix.os }}
    steps:
      - run: env # Debug
      - uses: actions/checkout@v6
      - run: ./.github/gh-actions-runner-xcode-select.sh
      - run: brew install bash fish xcbeautify swiftly pandoc # pandoc: build-docs.sh builds the man pages
      - run: swiftly init --skip-install --assume-yes --verbose && swiftly install
      - name: 'build-release.sh refuses to build ad-hoc without --allow-adhoc'
        run: |
          # Guards the guard: this must fail, and it fails fast (before any compilation).
          if ./script/build-release.sh --codesign-identity - ; then
            echo "build-release.sh accepted an ad-hoc signature without --allow-adhoc" >&2
            exit 1
          fi
      - name: './script/build-release.sh'
        run: |
          # "-" means "Sign to run locally". There is no aerospace-codesign-certificate on GH Actions,
          # so CI opts out of the certificate requirement with --allow-adhoc. Published releases must
          # NOT: an ad-hoc signature makes macOS drop the user's Accessibility and Screen Recording
          # grants on every upgrade (dev-docs/fork-maintenance.md).
          ./script/build-release.sh --codesign-identity - --allow-adhoc
          ./script/install-from-sources.sh --dont-rebuild
      - name: cat ./.release/xcodebuild.log
        if: ${{ always() }}
        run: 'if test -f ./.release/xcodebuild.log; then cat ./.release/xcodebuild.log; fi'
